# Backend Notes

See the root `README.md` for demos, media asset locations, and full project overview. This folder contains the FastAPI server; run it with `uvicorn main:app --reload` after installing `requirements.txt`.
